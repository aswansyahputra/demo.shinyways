docker build -f Dockerfile_base --progress=plain -t demo.shinyways_base .
docker build -f Dockerfile --progress=plain -t demo.shinyways:latest .
docker run -p 80:80 demo.shinyways:latest
# then go to 127.0.0.1:80